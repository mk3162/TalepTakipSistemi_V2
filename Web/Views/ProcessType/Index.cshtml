@model IEnumerable<Common.Models.Response.ResponseIslemTipleriDto>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/_AdminLayout.cshtml";
}

<hr />
<section class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-10 mx-auto ">
                <partial name="_AddExportPartialView" />

                <div class="card-body ">
                    <div id="example1_wrapper " class="dataTables_wrapper dt-bootstrap4 ">
                        <div class="row">
                            <div class="col-sm-12">
                                <table id="example1" class="table table-bordered table-striped dataTable dtr-inline" role="grid" aria-describedby="example1_info">
                                    <thead>
                                        <tr role="row">
                                            <th class="sorting" tabindex="0" aria-controls="example1" rowspan="1" colspan="1" aria-label="Platform(s): activate to sort column ascending">Sıra No</th>
                                            <th class="sorting" tabindex="0" aria-controls="example1" rowspan="1" colspan="1" aria-label="Platform(s): activate to sort column ascending">Tanımı</th>
                                            @*<th class="sorting" tabindex="0" aria-controls="example1" rowspan="1" colspan="1" aria-label="Platform(s): activate to sort column ascending">İşlemler</th>*@
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @foreach (var item in Model)
                                        {
                                            <tr>
                                                @*<td class="dtr-control sorting_1 text-center" tabindex="0">
                                                        @if (!String.IsNullOrWhiteSpace(item.Branch.Company.CompanyImageFile))
                                                        {<img class="img-fluid" src="@item.Branch.Company.CompanyImageFile" style="width:100px;" />}
                                                    </td>*@
                                                <td class="dtr-control sorting_1" tabindex="0">@item.SiraNo</td>
                                                <td class="dtr-control sorting_1" tabindex="0">@item.Tanimi</td>
                                                @*<td>
                                                    <a href="/ProcessType/GetProcessType/@item.SiraNo" class="btn btn-info fas fa-edit"></a>
                                                    @Html.ActionLink(" ", "SoftDelete","ProcessType", new { siraNo = item.SiraNo }, new { @class = "btn btn-danger fas fa-trash-alt", onclick = "return confirm('Silmek İstiyor musunuz ? ');" })
                                                </td>*@
                                            </tr>
                                        }
                                    </tbody>
                                </table>
                            </div>
                        </div>                     
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
<script src="~/TalepWeb/plugins/jquery/jquery.min.js"></script>

